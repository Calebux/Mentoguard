[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @mentoguard/shared build"

[deploy]
startCommand = "node --loader tsx/esm apps/telegram/src/bot.ts"
restartPolicyType = "always"
